<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="co.moreawesome.hugo.referral">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="co.moreawesome.hugo.referral.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="co.moreawesome.hugo.referral.MainActivity"
            android:label="@string/app_name">
        </activity>
        <activity
            android:name="co.moreawesome.hugo.referral.VetActivity"
            android:label="Dierenarts">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="co.moreawesome.hugo.referral.MainActivity"/>
        </activity>
        <activity
            android:name="co.moreawesome.hugo.referral.PetActivity"
            android:label="Patient">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="co.moreawesome.hugo.referral.MainActivity"/>
        </activity>
        <activity
            android:name="co.moreawesome.hugo.referral.OwnerActivity"
            android:label="Eigenaar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="co.moreawesome.hugo.referral.MainActivity"/>
        </activity>
    </application>

</manifest>